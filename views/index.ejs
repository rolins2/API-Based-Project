<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-T3c6CoIi6uLrA9TneNEoa7RxnatzjcDSCmG1MXxSR1GAsXEV/Dwwykc2MPK8M2HN" crossorigin="anonymous">
    <link  href="styles/main.css" rel="stylesheet"/>
</head>
<body>

    <header class="py-3 mb-4 border-bottom bggreen">
        <div class="container d-flex flex-wrap justify-content-center ">
          <a href="/" class="d-flex align-items-center mb-3 mb-lg-0 me-lg-auto link-body-emphasis text-decoration-none">
            <svg class="bi me-2" width="40" height="32"><use xlink:href="#bootstrap"></use></svg>
            <span class="fs-2  txtfam">The Weather App</span>
          </a>
          <form class="col-12 col-lg-auto mb-3 mb-lg-0" role="search">
            <i class = "text-white fs-4"> Get the weather data of any place you want </i>
          </form>
        </div>
      </header>
    <h2> Welcome to the api app</h2>
    <img src = "./icons/a02d.png">


    <form action="/getD" method="post">
        <label for="usrin">Enter the Name of the city that you want to know the current weather of </label>
        <input id="usrin" type = "text" name="inp" value = ""/>

        <button type="submit" >Check weather</button>


    </form>

   <% if  (locals.result) {%>

    <h2> The weather in -- <%= result.data[0].city_name %> is as follows:</h2>

    <p> The temperature is -- <%= result.data[0].app_temp %></p>

    <p>Date -- <%= result.data[0].datetime %> </p>

    <p> Air Quality Index :<%= result.data[0].aqi %> </p>


    <p>Sunrise time : <%= result.data[0].sunrise %></p>

    <p>Sunset Time <%= result.data[0].sunset %></p>

    <p>Wind degree <%= result.data[0].wind_cdir %></p>


    <p>  <%= result.data[0].weather.icon %> </p>

    <img src = " ./icons/<%= result.data[0].weather.icon %>.png" />


    <% } %>
</body>
</html>